# Registro de alteracoes - `.env.example`

Arquivo base: `backend/.env.example`
Arquivo de registro: `backend/.env.example.md`
Status no Git antes do commit: Novo
Data do registro: 2026-06-14
Area do projeto: Backend FastAPI do Mainha Assistant.
Tipo: Configuracao ou manifesto
Tamanho atual: 588 bytes; 20 linhas; sha256 curto: `d068550e33c29564`

## Alteracao registrada

Exemplo de ambiente atualizado para usar `GEMINI_MODEL=gemini-3.5-flash` e remover `GEMINI_API_BASE_URL`, refletindo a integracao nova com a SDK `google-genai`.

## Regra de manutencao

Este arquivo `.md` deve ser atualizado sempre que o arquivo base `backend/.env.example` receber qualquer alteracao antes de commit/push. Registre aqui o que mudou, por que mudou e qualquer impacto esperado em build, execucao, deploy, testes ou artefatos gerados.

## Observacoes para commit

- Conferir se o arquivo base deve mesmo entrar no commit.
- Para binarios e artefatos gerados, confirmar se o repositorio deve versionar esse conteudo.
- Para arquivos com tokens, endpoints ou configuracoes, confirmar que nenhum segredo real foi incluido.
